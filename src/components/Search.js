import React from "react";

const Search = () => {
  return (
    <div>
      <input
        className={`search-box`}
        type="search"
        placeholder="Search for a meal..."
      />
    </div>
  );
};

export default Search;
